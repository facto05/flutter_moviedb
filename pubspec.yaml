name: flutter_moviedb
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  bloc: ^8.0.0
  equatable: ^2.0.0
  dio: ^5.0.0
  flutter_bloc: ^8.0.0
  get_it: ^8.0.0
  intl: ^0.20.2
  json_serializable: ^6.10.0
  injectable: ^2.5.1
  json_annotation: ^4.9.0
  pretty_dio_logger: ^1.4.0
  go_router: ^16.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.6.0
  injectable_generator: ^2.5.1

flutter:
  uses-material-design: true
